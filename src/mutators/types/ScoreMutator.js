import Mutator from './Mutator'

// apply returns new game state with score changed according to rules
export default class ScoreMutator extends Mutator {
  constructor() {
    super()
  }

  apply() {}
}
